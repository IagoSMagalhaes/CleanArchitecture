package com.example.clean.architecture.entities.otherRepositories.domain.state.dto.request

open class RequestPostStateEntity(
                     val fullname: String?,
                     val initials: String?,

                   )
/*
fun RequestPostAssetEntity.toEntity() = AssetEntity(name = name,
                                           fullValue = fullValue,
                                            type = type,
                                           manager = manager,
                                           active = active)

 */
